---
name: {{componentName}}
menu: Components
route: /components-{{camelCase componentName}}
---

import { Playground, Props } from 'docz';
import Component from "react-component-component";
import { Row, Box } from './helpers';
import { {{componentName}} } from '../../lib';

# {{componentName}}

```javascript
import {{componentName}} from 'craigponent-library/dist/{{componentName}}';
```

TODO add description here

## Properties

<Props of={ {{~componentName~}} } />

## Basic Usage

<Playground>
  <Box style=\{{ background: "#fff" }}>
    <Component initialState=\{{}}>
      {({ setState, state }) => (
      <{{componentName}}>
      </{{componentName}}>
      )}
    </Component>
  </Box>
</Playground>

## Dark Mode

<Playground>
  <Component initialState=\{{}}>
    {({ setState, state }) => (
    <{{componentName}} styleMode="dark">
    </{{componentName}}>
    )}
  </Component>
</Playground>