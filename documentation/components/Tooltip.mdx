---
name: Tooltip
menu: Components
---

import { Playground, PropsTable } from 'docz'
import Component from "react-component-component";
import { Row } from './helpers'
import { Tooltip, Button, Input } from './../../src'
import { QuestionMark } from './../../src/icons'

# Tooltip

```javascript
import Tooltip from 'craig-component-library/dist/Tooltip'
```

Tooltips help clarify what certain features do.

### Best practices
- Design will define the best practices here

## Properties

<PropsTable of={Tooltip} />

## Basic usage

<Playground>
  <Row>
    <Tooltip content="Now you see me!">
      <Button style={{ marginBottom: 5 }}>
        Hover Me
      </Button>
    </Tooltip>
    <Tooltip hover={false} content="Now you see me!" position="bottom">
      <Button style={{ marginBottom: 5 }}>
        Click Me
      </Button>
    </Tooltip>
    <Tooltip
      arrow={false}
      content="Where did my arrow go?"
      position="bottom"
    >
      <Button style={{ marginBottom: 5 }}>
        No arrow
      </Button>
    </Tooltip>
    <Tooltip
      arrow={false}
      hover={false}
      content="Where did my arrow go?"
      position="bottom"
    >
      <Button style={{ marginBottom: 5 }}>
        No arrow Click
      </Button>
    </Tooltip>
  </Row>
</Playground>

## Positions

<Playground>
  {() => {
    const positions = ['top', 'top-left', 'top-right', 'bottom', 'bottom-left', 'bottom-right', 'left', 'right'];
    return (
      <Row>
        {positions.map(position => (
            <Tooltip
              key={position}
              position={position}
              content={`I am on the ${position}`}
              style={{ margin: '0px 10px' }}
            >
              {position.toUpperCase()}
            </Tooltip>
        ))}
      </Row>
    )
  }}
</Playground>

## Animations

<Playground>
  <Component
    initialState={{
      delay: 0,
      duration: 180,
      easing: 'ease-in-out',
    }}
  >
    {({ setState, state }) => {
      return (
        <>
          <Row>
            <Input
              label="Delay"
              placeholder="Delay"
              name="delay"
              type="number"
              onChange={(e) => setState({ delay: +(e.target.value) })}
              value={state.delay}
            />
            <Input
              label="Duration"
              placeholder="Duration"
              name="duration"
              type="number"
              onChange={(e) => setState({ duration: +(e.target.value) })}
              value={state.duration}
            />
            <Input
              label="Easing"
              placeholder="Easing"
              onChange={(e) => setState({ easing: e.target.value })}
              type="select"
              value={state.easing}
            >
              <option value="ease">ease</option>
              <option value="easing-in">easing-in</option>
              <option value="easing-out">easing-out</option>
              <option value="easing-in-out">easing-in-out</option>
              <option value="linear">linear</option>
              <option value="step-start">step-start</option>
              <option value="step-end">step-end</option>
            </Input>
          </Row>
          <div style={{ marginTop: '100px', textAlign: 'center' }}>
            <Tooltip
              content="Customize me"
              delay={state.delay}
              duration={state.duration}
              easing={state.easing}
            >
              <QuestionMark width="30px" height="30px" fillColor="#fff" />
            </Tooltip>
          </div>
        </>
      )
    }}
  </Component>
</Playground>
